//Declaração das variáveis
const raio = 5.6;
const area = Math.PI * Math.pow(raio,2) // primiero voce coloca o numero depois seu expoente

console.log(area.toFixed(2)); //toFixed para limitar o número de casas após a virgula
console.log(typeof Math); //Math com M maiusculo é um objeto(object)